<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Trail</title>
</head>
<body>
    <h1>Edit Trail</h1>
    <form action="/trails/<%= trail._id %>?_method=PUT" method="POST">
        <label for="name">Trail Name:</label>
        <input type="text" id="name" name="name" value="<%= trail.name %>" required><br>
    
        <label for="location">Location:</label>
        <input type="text" id="location" name="location" value="<%= trail.location %>" required><br>
    
        <label for="difficulty">Difficulty:</label>
        <select id="difficulty" name="difficulty" required>
            <option value="Easy" <%= trail.difficulty === 'Easy' ? 'selected' : '' %>>Easy</option>
            <option value="Moderate" <%= trail.difficulty === 'Moderate' ? 'selected' : '' %>>Moderate</option>
            <option value="Hard" <%= trail.difficulty === 'Hard' ? 'selected' : '' %>>Hard</option>
        </select><br>
    
        <label for="distance">Distance (miles):</label>
        <input type="number" id="distance" name="distance" value="<%= trail.distance %>" required><br>
    
        <label for="elevationGain">Elevation Gain (feet):</label>
        <input type="number" id="elevationGain" name="elevationGain" value="<%= trail.elevationGain %>"><br>
    
        <label for="description">Description:</label>
        <textarea id="description" name="description"><%= trail.description %></textarea><br>
    
        <label for="imageUrl">Image URL:</label>
        <input type="text" id="imageUrl" name="imageUrl" value="<%= trail.imageUrl %>"><br>
    
        <button type="submit">Update Trail</button>
    </form>
    

    <a href="/trails/<%= trail._id %>">Cancel</a>
</body>
</html>
